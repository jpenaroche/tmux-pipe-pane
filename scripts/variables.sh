# stored pipe variable prefix
PIPE_VAR_PREFIX="@pipe_prefix"

# basic pipe
default_pipe_key="k"
default_pipe_key_option="@pipe_key"

# reuse previous piped instruction
previous_pipe_instruction="S-K"
previous_pipe_instruction_option="@pipe_previous_key"